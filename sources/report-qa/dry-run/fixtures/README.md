# Report QA dry-run fixtures

This folder contains read-only Markdown fixtures for Report QA dry-run testing.

Fixtures are not runtime data and must not be used as proof that a live PILAR run is correct.

Current fixture:

```txt
realistic-steel-beam-report.md
```

Purpose:

```txt
Provide a more realistic, report-shaped input for Report QA dry-run work before connecting the QA logic to live PILAR report data.
```
